let initialState = {}
const searchReducer = function (state = initialState, action) {
    switch (action) {
        case "":
            return state
        default:
            return state
    }
}

export default searchReducer;