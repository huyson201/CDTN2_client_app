import React from "react";
import { Modal, View } from "react-native";
import { Text } from "react-native-elements";

const LocalModal = function () {
    return (
        <Modal>
            <View>
                <Text>Modal</Text>
            </View>
        </Modal>
    )
}

export default LocalModal;